module.exports = {
    stringCalculator: async (numberString) => {
        if(numberString.length === 0) {
            return 0;
        }
        if(numberString.length === 1) {
            return numberString;
        }
    }
}